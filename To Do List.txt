npm install --save-dev babel-plugin-styled-components
npm install @plaiceholder/next
npm ls


Fixes:
    Index Page:
        -replace index page collage picture to image which has much smaller images inside
        -add scroll to top button
        -for now,just add 5 predefined users to login,and show their content(with localstorage memorizng fav images and hitory) and for registration just Say th like 'registration is uscceful but is not saved'
        -add catch error for every fetch stuff